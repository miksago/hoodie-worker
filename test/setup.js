var assert = require("assert");
var Worker = require("./../lib/setup");

describe("Setup", function() {
  describe("foo()", function() {
    it("should do that thing", function() {
      assert(true);
    });
  });
});